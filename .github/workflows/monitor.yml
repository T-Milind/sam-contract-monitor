name: SAM.gov IT Contract Monitor

on:
  schedule:
    # Spread across US business hours (~8am/11am/2pm/5pm US Eastern)
    - cron: "0 13 * * *"
    - cron: "0 16 * * *"
    - cron: "0 19 * * *"
    - cron: "0 22 * * *"
  workflow_dispatch: {}

concurrency:
  group: sam-monitor
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: DIAGNOSTIC - curl sam.gov
        run: |
          curl -sv -m 25 "https://sam.gov/api/prod/sgs/v1/search/?random=1&index=ac&page=0&sort=-modifiedDate&size=1&mode=search&responseType=json&domain=ac&q=information%20technology&qMode=ALL" -H "User-Agent: Mozilla/5.0" -o /tmp/resp.json || echo "CURL_EXIT=$?"
          echo "--- response head ---"
          head -c 500 /tmp/resp.json || true

      - name: Run monitor
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          RECIPIENT_EMAILS: ${{ secrets.RECIPIENT_EMAILS }}
        run: python main.py

      - name: Commit updated state
        run: |
          git config user.name "sam-monitor-bot"
          git config user.email "actions@github.com"
          git add state/seen_ids.json
          git diff --staged --quiet || git commit -m "Update seen-notice state [skip ci]"
          git push
