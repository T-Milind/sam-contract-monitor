# Copy to .env for local testing (never commit .env). In GitHub Actions these
# are set as repo secrets instead (Settings -> Secrets and variables -> Actions).

GEMINI_API_KEY=your_ai_studio_api_key
GMAIL_ADDRESS=youraddress@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password
RECIPIENT_EMAILS=t.milind2k3@gmail.com

# Optional overrides (defaults shown)
# GEMINI_MODEL_STAGE1=gemini-2.5-flash
# GEMINI_MODEL_STAGE2=gemini-2.5-flash
# STAGE1_THRESHOLD=7.5
# SAM_QUERY=information technology
